# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key

# Backend: omit for local dev (Vite proxies /api/*) or Docker prod (nginx proxies /api/*).
# Set full URLs only for hosts without a proxy (e.g. some Render setups).
# VITE_EXERCISE_API_URL=
# VITE_WORKOUT_API_URL=
# VITE_ASSISTANT_API_URL=
