services:
  - type: web
    name: ai-workout-suggestion-api
    env: docker
    region: oregon
    plan: free
    branch: main
    rootDir: AI-Workout-Suggestion-main
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
